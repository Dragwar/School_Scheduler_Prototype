@model School_Scheduler.MVC.Models.ViewModels.CalenderDataViewModel

@{
    ViewBag.Title = "Home Page";
}

<style>
    .calenderContainer {
        display: flex;
        flex-flow: row wrap;
    }
</style>

@*<div>@Model.SchoolPrograms[0].Instructors[0].Name</div>*@

@*<div class="calenderContainer">
    @for (int i = 0; i < 12; i++)
    {
        var daysInMonth = DateTime.DaysInMonth(Model.Today.Year, i);
        for (var j = 0; j < daysInMonth; j++)
        {
            <div class="calenderChild">
                <p>@(j+1)</p>
                <p></p>
            </div>
        }
    }
    
        @foreach(var schoolProgram in Model.SchoolPrograms)
        {
            <div>
            </div>
        }
    
</div>*@

@*@{ 
    Layout = null;
}*@

@*<div class="col-xl-3 col-lg-6">
    <div class="card card-stats mb-4 mb-xl-0">
        <div class="card-body">
            <div class="row">
                <div class="col">
                    <h5 class="card-title text-uppercase text-muted mb-0">My Courses</h5>
                    <span class="h2 font-weight-bold mb-0">350,897</span>
                </div>
                <div class="col-auto">
                    <div class="icon icon-shape bg-danger text-white rounded-circle shadow">
                        <i class="fas fa-chart-bar"></i>
                    </div>
                </div>
            </div>
            <p class="mt-3 mb-0 text-muted text-sm">
                <span class="text-success mr-2"><i class="fa fa-arrow-up"></i> 3.48%</span>
                <span class="text-nowrap">Since last month</span>
            </p>
        </div>
    </div>
</div>*@